# vaxlink-scanner-agent

Binary entrypoint lives at `src/main.rs` so Cargo can build this package directly. This directory is kept to match the native-agent project layout and to hold future packaging or wrapper files for the installed command.
