# Dev server config for JupyterHub server-proxy environment
# Tells Next.js to prefix all asset URLs with the proxy base path
# so CSS/JS load correctly through the proxy.
BASE_PATH=/notebook/analytics/vaxlink/proxy/3000
PORT=3000
