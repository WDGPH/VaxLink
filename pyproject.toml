[project]
name = "vaxlink-docs"
version = "0.1.0"
description = "Documentation tooling for VaxLink (mkdocs-material)"
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }
dependencies = []

[dependency-groups]
docs = [
  "mkdocs>=1.6.0",
  "mkdocs-material>=9.5.15",
  "pymdown-extensions>=10.21.2",
]
